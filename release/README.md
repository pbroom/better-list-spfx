# better-list-spfx Release

This folder contains flat runtime assets generated by the production build.

Tagged releases place these files in a URL-agnostic CDN deployment kit. The
kit materializer binds its non-deployable package template to the final HTTPS
CDN base path selected during deployment.
